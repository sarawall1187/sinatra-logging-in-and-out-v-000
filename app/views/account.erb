<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">
  </head>
  <body id="page-top">
    <% if @user.is_logged_in? %>
    <h1><%= @user.current_user.username %></h1>
    <h1><%= @user.current_user.email %></h1>
    <h1><%= @user.current_user.balance %></h1>
    <a href="/logout">Log Out</a>
    <% end %>

  </body>
</html>
